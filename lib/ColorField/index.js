import React from 'react';

import { classNames } from '../utils';

const ColorField = ({
  className,
}) => {

  return (
    <div
      className={classNames(
        'junipero',
        ''
      )}
    >

    </div>
  );
};

export default ColorField;
